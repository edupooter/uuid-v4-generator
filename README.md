Gera um UUID (Universal Unique Identifier) na versão 4 (números completamente aleatórios).

Baseado no pacote 'ramsey/uuid' que deve ser instalado com:

`composer install`

Usar com code runner ou:

`php ./generator.php`